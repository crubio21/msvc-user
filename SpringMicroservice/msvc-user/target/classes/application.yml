server:
  port:8090

spring:
  application:
    name: msvc-user
  datasource:
    driver-class-name: com.mysql-cj-jdbc.Driver
    url: jdbc:mysql://localhost:3366/usersDb
    username: root
    password:
  jpa:
    hibernate:
      ddl-auto: create
    database: mysql
    database-platform: org.hibernate.dialect.MySQL8Dialect
